<Window x:Class="VideoWall.Master.AddAgentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Agent"
        Height="480" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Connect to Agent" Style="{StaticResource SubHeaderStyle}" Margin="0,0,0,8"/>

        <!-- Discovery status -->
        <TextBlock x:Name="txtDiscoveryStatus" Grid.Row="1" Style="{StaticResource MutedStyle}" Margin="0,0,0,8"
                   Text="Scanning for agents on LAN..."/>

        <!-- Discovered agents list -->
        <Border Grid.Row="2" Style="{StaticResource PanelStyle}" Padding="0" Margin="0,0,0,12">
            <ListView x:Name="lstDiscoveredAgents" SelectionChanged="DiscoveredAgent_SelectionChanged">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Hostname" Width="140" DisplayMemberBinding="{Binding ComputerName}"/>
                        <GridViewColumn Header="IP Address" Width="130" DisplayMemberBinding="{Binding IpAddress}"/>
                        <GridViewColumn Header="Port" Width="60" DisplayMemberBinding="{Binding GrpcPort}"/>
                        <GridViewColumn Header="Outputs" Width="60" DisplayMemberBinding="{Binding OutputCount}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- Separator -->
        <TextBlock Grid.Row="3" Text="Or enter manually:" Style="{StaticResource MutedStyle}" Margin="0,0,0,8"/>

        <!-- Manual entry -->
        <Grid Grid.Row="4" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="IP Address" Style="{StaticResource MutedStyle}" Margin="0,0,0,4"/>
            <TextBox x:Name="txtIpAddress" Grid.Row="1" Text="192.168.1.100" Margin="0,0,8,0"/>

            <TextBlock Grid.Column="1" Text="Port" Style="{StaticResource MutedStyle}" Margin="0,0,0,4"/>
            <TextBox x:Name="txtPort" Grid.Row="1" Grid.Column="1" Text="5100"/>
        </Grid>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="Cancel_Click" Margin="0,0,8,0" Width="80"/>
            <Button Content="Connect" Style="{StaticResource PrimaryButton}" Click="Connect_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>
